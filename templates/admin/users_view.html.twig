{% extends 'base.html.twig' %}

{% block title %}Informations utilisateur
{% endblock %}

{% block content %}
	<div class="banniere-find">
        <img src="{{asset('asset/img/hollywood.jpg')}}" alt="admission" id="img-admission">
    </div>  
	<h1 class="text-center mt-5 pt-5">Informations utilisateur</h1>

<div class="container user-view">
	<div class="row justify-content-center">
		<div class="col-8">
			<div class="table-responsive">
				<table class="table" style="color: white">
					<thead>
						<tr>
							<th>id</th>
							<th>Pseudo</th>
							<th>Prénom</th>
							<th>Nom</th>
							<th>Email</th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<td>{{user.id}}</td>
							<td>{{user.pseudo}}</td>
							<td>{{user.firstname}}</td>
							<td>{{user.lastname}}</td>
							<td>{{user.email}}</td>
						</tr>
					</tbody>
				</table>

				{# Bouton de retour #}
				<div class="text-center return mb-5 pb-5">
					<a href="{{ path('admin_list') }}" class="btn bg_perso p-2">Retour</a>
				</div>

			</div>
		</div>
	</div>
</div>
	{% endblock %}
